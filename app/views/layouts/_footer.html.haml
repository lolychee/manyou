= yield :footer_before
#footer
  .wrapper
    = yield :footer_prepend

    #bottom-menu
      %ul
    #copyright
      @ Copyright
      //= link_to APP_CONFIG['site_name'], root_path
      |
      Power by
      = link_to 'manyou', 'https://github.com/lolychee/manyou', :target => '_blank'

    = yield :footer_append

= yield :footer_after

